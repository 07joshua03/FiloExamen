<style>
  #fuck{
      height: 500px !important;
  }
</style>

<%= form_with(model: end_term) do |form| %>
  <% if end_term.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(end_term.errors.count, "error") %> prohibited this end_term from being saved:</h2>

      <ul>
        <% end_term.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    Eindterm:
    <%= form.number_field :number, class:"form-control" %>
  </div>

  <div class="field form-group">
    Hoofdstuk:
    <%= form.number_field :chapter, class:"form-control" %>
  </div>

  <div class="field form-group">
    <div class="input-group" >
      <div class="input-group-prepend">
        <span class="input-group-text"><%= form.label :definition %></span>
      </div>
      <%= form.text_area :definition, class:"form-control", id:"fuck" %>
    </div>
  </div>

  <div class="actions form-group">
    <%= form.submit "Submit!", class:"form-control"%>
  </div>
<% end %>
