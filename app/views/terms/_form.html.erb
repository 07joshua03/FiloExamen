<style>
    #fuck{
        height: 500px !important;
    }
</style>

<%= form_with(model: term) do |form| %>
  <% if term.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(term.errors.count, "error") %> prohibited this term from being saved:</h2>

      <ul>
        <% term.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    Begrip:
    <%= form.text_field :name, class:"form-control" %>
  </div>

  <div class="field form-group">
    Hoofd eindterm:
    <%= form.number_field :end_term, class:"form-control" %>
  </div>

  <div class="field form-group">
    Andere eindtermen:
    <%= form.text_area :other_et, class:"form-control" %>
  </div>

  <div class="field form-group">
    <div class="input-group" >
      <div class="input-group-prepend">
        <span class="input-group-text">Definitie:</span>
      </div>
      <%= form.text_area :definition, class:"form-control", id:"fuck" %>
    </div>
  </div>

  <div class="actions form-group">
    <%= form.submit %>
  </div>
<% end %>
